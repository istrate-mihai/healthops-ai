[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_URL = "https://healthops-ai-backend.onrender.com"  # update after Render deploy
